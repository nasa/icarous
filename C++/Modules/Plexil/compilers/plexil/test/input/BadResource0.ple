Integer Command c1;

Root: {
  Integer returnValue = -1;
  EndCondition returnValue == 10;
  Resource Name = "arm"; // missing Priority
  Resource Name = "leg", Priority = 20, Priority = 30; // duplicate Priority
  returnValue = c1();
}
