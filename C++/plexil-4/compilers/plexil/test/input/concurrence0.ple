// Note that it takes at least two actions to generate a Concurrence.  This one,
// while syntactically valid, is logically invalid because we can't assign to
// the same variable twice in concurrence.

Concurrence {
  Integer x;
  x = 0;
  x = 1;
}
