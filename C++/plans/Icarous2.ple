// -*- Mode: Java -*-

Boolean Lookup missionStart;
Command pprint(...);

ICAROUS:
Concurrence
{
    FMS:
    Concurrence
    {

	pprint("Starting FMS\n");
	
	IDLE:
	{
	    Repeat true;
	    EndCondition Lookup(missionStart);
	}

	TAKEOFF:{
	    Repeat true;
	    pprint("Takeoff\n");
	   
	}
    }
}
